import { ApiRoutes } from './Routes';

const Headers = {
  json: {
    'Content-Type': 'application/json',
  },
  multipart: {
    'Content-Type': 'multipart/form-data',
  },
};

export { Headers, ApiRoutes };
